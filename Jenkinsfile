pipeline{
agent any
stages{
     stage("get code"){
	    steps{
		      echo "get code scm"
		}
	 }
	 stage("package"){
	    steps{
		      echo "package"
		}
	  } 
	  stage("deploy"){
	    steps{
			  echo "deploy" 
		}
	  } 
    }  
}
